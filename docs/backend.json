{
  "entities": {
    "Employee": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Employee",
      "type": "object",
      "description": "Represents an employee in the system.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The full name of the employee."
        },
        "position": {
          "type": "string",
          "description": "The job title or position of the employee.",
          "enum": [
            "pelayanan",
            "admin",
            "pengantaran",
            "staff gudang"
          ]
        },
        "joinDate": {
          "type": "string",
          "format": "date",
          "description": "The date the employee started working."
        },
        "phone": {
          "type": "string",
          "description": "The employee's phone number."
        },
        "salary": {
          "type": "number",
          "description": "The employee's monthly salary."
        },
        "status": {
          "type": "string",
          "description": "The employment status of the employee.",
          "enum": [
            "aktif",
            "tidak aktif"
          ]
        }
      },
      "required": [
        "name",
        "position",
        "joinDate",
        "phone",
        "salary",
        "status"
      ]
    },
    "AttendanceRecord": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AttendanceRecord",
      "type": "object",
      "description": "Records an employee's clock-in and clock-out times for a specific period.",
      "properties": {
        "employeeId": {
          "type": "string",
          "description": "Reference to the Employee who made this attendance record."
        },
        "clockIn": {
          "type": "string",
          "description": "The date and time when the employee clocked in.",
          "format": "date-time"
        },
        "clockOut": {
          "type": "string",
          "description": "The date and time when the employee clocked out. This can be null if the employee has not yet clocked out.",
          "format": "date-time"
        },
        "notes": {
          "type": "string",
          "description": "Optional notes or remarks for this attendance record."
        }
      },
      "required": [
        "employeeId",
        "clockIn"
      ]
    },
    "Payroll": {
      "title": "Payroll",
      "description": "Represents a payroll run for a specific period.",
      "type": "object",
      "properties": {
        "period": {
          "type": "string",
          "description": "The payroll period, formatted as YYYY-MM."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the payroll was created."
        },
        "status": {
          "type": "string",
          "enum": [
            "draft",
            "finalized"
          ],
          "description": "The status of the payroll run."
        }
      },
      "required": [
        "period",
        "createdAt",
        "status"
      ]
    },
    "Payslip": {
      "title": "Payslip",
      "description": "Represents an individual employee's payslip for a specific payroll period.",
      "type": "object",
      "properties": {
        "employeeId": {
          "type": "string",
          "description": "Reference to the Employee."
        },
        "employeeName": {
          "type": "string",
          "description": "The name of the employee at the time of payroll."
        },
        "baseSalary": {
          "type": "number",
          "description": "The employee's base salary for the period."
        },
        "lateCount": {
          "type": "number",
          "description": "Number of times the employee was late during the period."
        },
        "lateDeduction": {
          "type": "number",
          "description": "Total deduction amount for being late."
        },
        "netSalary": {
          "type": "number",
          "description": "The final net salary after all deductions."
        }
      },
      "required": [
        "employeeId",
        "employeeName",
        "baseSalary",
        "lateCount",
        "lateDeduction",
        "netSalary"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/employees/{employeeId}",
        "definition": {
          "entityName": "Employee",
          "schema": {
            "$ref": "#/backend/entities/Employee"
          },
          "description": "Stores individual employee profiles."
        }
      },
      {
        "path": "/attendance/{attendanceRecordId}",
        "definition": {
          "entityName": "AttendanceRecord",
          "schema": {
            "$ref": "#/backend/entities/AttendanceRecord"
          },
          "description": "Stores all attendance records (clock-in/clock-out) for all employees in a single collection for easier querying."
        }
      },
      {
        "path": "/payrolls/{payrollId}",
        "definition": {
          "entityName": "Payroll",
          "schema": {
            "$ref": "#/backend/entities/Payroll"
          },
          "description": "Stores payroll runs for each period."
        }
      },
      {
        "path": "/payrolls/{payrollId}/payslips/{payslipId}",
        "definition": {
          "entityName": "Payslip",
          "schema": {
            "$ref": "#/backend/entities/Payslip"
          },
          "description": "Stores individual employee payslips for a given payroll run."
        }
      }
    ]
  }
}
